export const mean = (num1: number, num2: number): number => {
    return (num1 + num2) / 2
}

// const testNum1 = 4;
// const testNum2 = 2;
// console.log(`Hello, the mean of ${testNum1} and ${testNum2} is ${mean(testNum1, testNum2)}`);